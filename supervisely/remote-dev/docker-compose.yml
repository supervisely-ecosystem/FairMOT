version: "2.2"
services:
  fairmot:
    shm_size: '2gb'
    runtime: nvidia
    build:
      context: .
      args:
        IMAGE: continuumio/miniconda
    ports:
      - "1234:22"
    volumes:
      - "./data:/data"
