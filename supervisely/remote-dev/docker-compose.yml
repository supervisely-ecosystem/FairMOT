
version: "2.2"
services:
  fairmot:
    shm_size: '8gb'
    runtime: nvidia
    build:
      context: .
      args:
        IMAGE: nvidia/cuda:11.1.1-devel-ubuntu18.04 --default-runtime=nvidia
    ports:
      - "1234:22"
    volumes:
      - "./data:/data"
